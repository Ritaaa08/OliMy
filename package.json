{
  "name": "olimy-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine de la sandwicherie vietnamienne OliMy, Lausanne",
  "type": "module",
  "scripts": {
    "images": "python3 tools/optimize-images.py",
    "build": "node src/build.js",
    "serve": "node serve.js",
    "dev": "node src/build.js && node serve.js",
    "apercu": "PREVIEW=1 node src/build.js && rm -rf apercu && cp -R site apercu"
  },
  "engines": {
    "node": ">=20"
  }
}
